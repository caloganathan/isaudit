import type { Metadata } from "next";
import AppendixDoc from "@/components/AppendixDoc";

export const metadata: Metadata = {
  title: "JCSS Indonesia — Appendix: Regulatory Register & Engagement Reference",
};

export default function AppendixPage() {
  return <AppendixDoc />;
}
